<% flash.each do |message_type, message| %>
<div class="row">
	<div class="large-12 columns">
		<div class="alert-box <%= message_type %> radius">
			<%= message %>
			<a href="#" class="close">&times;</a>
		</div>
	</div>
</div>
<% end %>