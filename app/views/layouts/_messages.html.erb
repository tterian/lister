<div class="row message-box">
	<div class="large-12 columns">
		<% flash.each do |message_type, message| %>
			<div data-alert class="alert-box radius <%= message_type %>">
				<%= message %>
				<a href="#" class="close">&times;</a>
			</div>
		<% end %>
	</div>
</div>