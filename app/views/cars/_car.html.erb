<li id="car-<%= car.id %>">		
	<div class="content">
		<div class="left">
			<div class="car-title">
				<b> 
					<%= Make.where(id: Model.where(id: car.model_id).first.make_id).first.name %>
					<%= Model.where(id: car.model_id).first.name %>
				</b>
			</div>
			<div class="car-thumb">
				<%= image_tag car.picture.thumb.url if car.picture? %>
			</div>
			<div class="car-timestamp">
				<p>added <%= time_ago_in_words(car.created_at) %> ago
	    		<% if current_user?(car.user) %>
	    			<%= link_to "delete", car, method: :delete, data: { confirm: "You sure?" } %>
	    		<% end %>
	    		</p>
	    	</div>
	    </div>

	    <div class="car-price right">
	    	<%= car.price %>
	 	</div>
	</div>
</li>
<hr>
